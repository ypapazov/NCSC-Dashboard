# Dev overrides: expose debug ports for direct access.
services:
  fresnel:
    ports:
      - "8080:8080"
    environment:
      LISTEN_ADDR: ":8080"

  postgres:
    ports:
      - "5432:5432"

  keycloak:
    ports:
      - "8081:8080"  # Direct admin console access during development
